// import type { Metadata } from "next";

// metadata.tsx
const Metadata = () => {
  return (
    <head>
      <title>Qurela</title>
      <meta name="description" content="Qurela" />
      {/* Other metadata */}
    </head>
  );
}

export default Metadata;
